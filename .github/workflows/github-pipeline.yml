name: github-pipeline
on: [push]
jobs:
  checks-code:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v3
      - name: hello world 
        run: | 
          echo "hello"
          ls
          pwd